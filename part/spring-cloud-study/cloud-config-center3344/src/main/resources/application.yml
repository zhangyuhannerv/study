server:
  port: 3344
spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/takatsukun/study-spring-cloud-config.git
          #使用如下地址需要配置ssh(麻烦，不想搞)
          #          uri: git@github.com:Takatsukun/study-spring-cloud-config.git
          username: Takatsukun
          password: Zh13Yu55Ha16
      #如果需要访问子目录那么需要添加该配置项
      #          search-paths:
      #            - springcloud-config
      label: master
  rabbitmq:
    host: 192.168.220.20
    port: 5672
    username: admin
    password: 123
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka,http://localhost:7002/eureka
#暴露bus刷新配置的端点
management:
  endpoints:
    web:
      exposure:
        include: 'bus-refresh'

